type Query {
  getUser(id: String!): User
}

type Mutation {
  register(
    firstName: String!, 
    lastName: String!,
    email: String!, 
    password:String!
  ): String
}

type User {
  id: String
  firstName: String
  lastName: String
  password: String
}